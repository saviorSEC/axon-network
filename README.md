# AXON NETWORK — Internal Infrastructure Galaxy

Flock-globe-style 3D internal-network galaxy for the Axon Enterprise footprint.
**Planets = zones**, **satellites = services**, **orbiting pools = cloud CIDRs**,
**directional particles = data streams** between planets.

- 9 planets / 54 services + 9 IP pools / 84 links
- Click any planet → member services (drill-in) · click service → role/DNS/certs/keys/endpoints
- Deep link: `?focus=nodeId`
- **Boundary:** passive OSINT — CT logs, DNS, frontend bundles, benign probes. TLP:AMBER (creds redacted on public render). Full archive: saviorSEC/axon (private).

Data: `axon_network.js` (machine-readable `AXON_INT` = `{zones, services, ipPools, links}`)
